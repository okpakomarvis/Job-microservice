spring:
  application:
    name: 'config-server'
  cloud:
    config:
      server:
        git:
          uri: 'https://github.com/okpakomarvis/applicationconfig.git'
#euraka client
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: 'http://localhost:8761/eureka/'